/vendor/
/tests/coverage
/.phpunit.cache/
/.php-cs-fixer.cache
phpunit.xml

# local agent context (not shipped)
/AGENTS.md
/CLAUDE.md

# local NPI-derived validation corpus (real provider names, not shipped)
/corpus-npi.tsv

# codesage local index + review state (machine-specific, not shipped)
/.codesage/
